{
  "name": "@highlightjs/<%= props.type %>-<%= props.slug %>",
  "version": "0.0.0",
  "description": "<%= props.description %>",
  "author": "<%= props.author %>",
  "license": "<%= props.license %>",
  "main": "<%= props.slug %>.css",
  "files": [
    "<%= props.slug %>.css"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/highlightjs/<%= props.type %>-<%= props.slug %>.git"
  },
  "keywords": [
    "highlight.js",
    "highlightjs",
    "highlightjs-style",
    "hljs",
    "<%= props.slug %>"
  ],
  "dependencies": {},
  "devDependencies": {
    "husky": "^3.0.8",
    "stylelint": "^11.1.1",
    "stylelint-config-recommended": "^3.0.0",
    "util": "^0.12.1"
  },
  "scripts": {
    "lint": "stylelint <%= props.slug %>.css",
    "lint:fix": "stylelint <%= props.slug %>.css --fix",
    "test": "npm run lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  }
}
